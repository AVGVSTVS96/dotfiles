{
  "version": 1,
  "files": [
    {
      "target": "dist/package-manager-cli.js",
      "replacements": [
        {
          "anchorHint": "end of the pi self-update branch, immediately after Updated ${APP_NAME}",
          "oldText": "                    console.log(chalk.green(`Updated ${APP_NAME} from ${VERSION} to ${selfUpdatePlan.version}`));\n                }\n                return true;\n",
          "newText": "                    console.log(chalk.green(`Updated ${APP_NAME} from ${VERSION} to ${selfUpdatePlan.version}`));\n                    try { (await import(\"node:child_process\")).spawnSync(\"pi-patcher\", [\"reconcile\"], { stdio: \"inherit\" }); } catch { }\n                }\n                return true;\n"
        }
      ]
    }
  ]
}
